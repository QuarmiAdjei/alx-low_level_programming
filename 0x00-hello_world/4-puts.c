#include <stdio.h>
/**
 * main - A code that prints the out of a C language
 * Return: Always 0 (this makes it right)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
